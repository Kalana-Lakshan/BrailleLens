# Train the cell detector on Colab / Kaggle

This file is a short pointer. The copy-paste Colab runbook for **both**
Stage 4a (cell YOLO) and Stage 4b (CNN) lives at the repo root:

**[`colab_training.md`](../colab_training.md)**

This PC is CPU-only. Real training is GPU-only. Do not mix the two jobs:
use a separate Colab notebook (and a separate Drive folder) for each.
