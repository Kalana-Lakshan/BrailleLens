# Dataset (clone separately — see README)
data DBSI/
AngelinaDataset-master/
data Angelina/

# Archive older CNN checkpoints (keep checkpoints/ as the only live folder)
# Duplicate .pt files that were un-tracked leftover copies.
braille_cnn/checkpoints/archive/

# Python cache
__pycache__/
*.pyc

# Virtual environment (recreate with: uv sync)
.venv/

# Debug / temp outputs
debug_*.png

# Secrets
.env
test-img_*_dots.png
test-img_yolo*.png

# YOLO local datasets / Colab upload blobs (regenerate or use Drive)
yolo_dot_detect/datasets/
yolo_dot_detect/runs/
yolo_dot_detect/colab_upload/*.zip
yolo26n.pt
yolov8n.pt
yolo_dot_detect/test_images/output/
yolo_dot_detect/test_images/output_dotneuralnet/

# Stage 2–4 generated data (rebuild with the data_pipeline / cell_detect CLIs)
data_pipeline/crops/
data_pipeline/manifests/
cell_detect/datasets/
cell_detect/runs/
cell_detect/weights/*.pt
cell_detect/**/*.pt
cell_detect/**/*.zip
colab_upload/

# Trained CNN weights (large; send back from Colab, do not commit)
braille_cnn/checkpoints/*.pt
braille_cnn/checkpoints/*.npy

# Nested clone leftovers
experiments/DotNeuralNet/.git/

# finger_cell_track local venv (MediaPipe isolated from system TF/protobuf)
finger_cell_track/.venv/
finger_cell_track/_*.png
# Large tip datasets + training outputs (download locally / on Kaggle)
finger_cell_track/datasets/
finger_cell_track/runs/
finger_cell_track/weights/
finger_cell_track/**/*.pt
finger_cell_track/**/*.zip
